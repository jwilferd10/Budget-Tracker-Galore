const APP_PREFIX = 'NAME';
const VERSION = 'version_01';
const CACHE_NAME = APP_PREFIX + VERSION;

const FILES_TO_CACHE = [];

// self.addEventListener('activate', function(e) {})

// self.addEventListener('fetch', function (e) {})